# Leçons apprises

| Date | Ce qui a mal tourné | Règle pour l'éviter |
|------|---------------------|---------------------|
| 2026-04-14 | — | Projet initialisé, aucune leçon encore |
| 2026-04-15 | Opérations bloquantes (psutil, extraction PDF, YAKE) dans l'event loop FastAPI → freeze du streaming HTTP | Toujours wrapper les appels bloquants avec `await loop.run_in_executor(None, fn, *args)` dans les endpoints async |
| 2026-04-15 | sentence-transformers sur Apple Silicon tentait d'utiliser MPS (GPU Metal) → instabilité + consommation mémoire partagée | Forcer `device="cpu"` dans le singleton Embedder local ; ne pas laisser le framework choisir automatiquement sur macOS |
| 2026-04-15 | Hook `block_main_branch.py` bloque les Edit/Write sur la branche `main` | Créer une branche feature (`git checkout -b docs/...`) avant toute modification, même pour la doc |
| 2026-04-15 | `local_indexer.py` n'insère pas `abs_path` dans le payload — divergence avec server/indexer.py | Quand deux modules produisent le même format Qdrant, extraire les constantes de payload dans shared/schema.py ou documenter explicitement les différences dans DECISIONS.md |
| 2026-04-16 | Token Cloudflare Tunnel partagé en clair dans une conversation IA | Les secrets (tokens, API keys) doivent **uniquement** passer par `.env` gitignored. Ajouter `.gitignore` dès la création du repo. Si un token a été exposé : le révoquer/rotater immédiatement sur le dashboard concerné. |
| 2026-04-16 | ngrok free : URL random + rate limit + page d'avertissement + session expirée | Pour tout tunnel récurrent, préférer Cloudflare Tunnel named (URL stable, gratuit, pas de limite). ngrok reste utile pour du one-shot rapide. |
| 2026-04-16 | Reprise doc-level + flush mid-doc = silent data loss (chunks manquants dans un doc marqué "indexé") | Flush atomique à la frontière de document. Un doc est soit 100 % en Qdrant soit 0 %, jamais entre les deux. |
| 2026-04-16 | `wait=False` sur Qdrant `upsert` → ACK avant persistence → perte silencieuse si crash Qdrant | Utiliser `wait=True` pour les indexations critiques. Le coût perf (~5-10 %) est négligeable comparé à une perte de données. |
| 2026-04-16 | YAKE (pur Python) sur CPU Mac faible = goulot d'étranglement | Toujours déporter les calculs CPU-heavy vers la ressource la plus performante. Bande passante internet ≫ CPU Mac âgé. |
| 2026-04-16 | `fitz.open()` sans context manager = fuite mémoire sur exception | Toujours utiliser `with fitz.open()` pour les ressources externes (PDF, DB connections, file handles). |
| 2026-04-16 | `transformers ≥ 4.50` bloque `torch.load` dès `torch < 2.6` (CVE-2025-32434) → BGE-M3 inchargeable sur Intel Mac (x86_64 plafonné à torch 2.2.2, support droppé) | Pinner `transformers < 4.50` tant que l'on reste sur Intel Mac, ou migrer vers un environnement ARM/Linux si l'on veut bénéficier des versions récentes. À terme : faire porter l'encodage des requêtes à Colab aussi (Mac reste stateless). |
| 2026-04-17 | Qdrant `FusionQuery(RRF)` natif fusionne au niveau **chunk** → un gros doc multi-chunks accumule N entrées dans chaque canal face à un doc 1-chunk pertinent → petits docs pertinents disparaissent du top-N (cas Giraud_Aure ordonnance introuvable) | En recherche documentaire, toujours faire la fusion RRF au niveau **document** : dédupliquer par `doc_id` dans chaque canal (garder le meilleur chunk) AVANT la fusion. Qdrant natif ne le fait pas — fusion manuelle requise. |
| 2026-04-17 | RRF pur ne remonte pas un doc 1-chunk dont le nom de fichier contient des mots-clés très discriminants (ex. "ordonnance-de-medicaments.pdf" pour "liste de médicaments") | Ajouter un **filename/title boost** en canal virtuel : pour chaque token requête (≥4 chars, accents retirés via NFD) présent dans `path`/`title`, ajouter `1/RRF_K` (≈0.0167) au score RRF. Signal très fiable, sans risque de faux positifs. |
| 2026-04-17 | Filename boost sur tokens génériques ("documents", "liste", "page") ramène des faux positifs ; acronymes courts ("PV", "SEO") exclus du boost par seuil ≥4 chars | (1) Ajouter une **stopword list FR/EN** sur les tokens génériques. (2) **Heuristique acronyme** : accepter tokens de 2-3 chars SI écrits en majuscules dans la requête originale (`\b[A-Z]{2,}\b`). (3) **Pénalité de rareté IDF-like** : si un token apparaît dans >30 % du pool candidat, diviser son poids par 2 (token peu discriminant). |
| 2026-04-17 | Test ciblé (test_targeted.py) matchait les paths par substring brute → "prevoyance axa" ne matchait pas "prévoyance axa" (accents) | Toujours normaliser via `unicodedata.normalize("NFD", s).lower()` + filter catégorie "Mn" côté test ET côté server/search.py pour cohérence. |
| 2026-04-18 | Grid-search 41 configs × 145 queries : tous les variants cluster à MRR 0.71-0.75, winner +0.006 seulement. Le code rarity_factor/rarity_threshold produit des MRR **identiques au 4e chiffre** sur 7 variantes → no-op effectif. Et 19 requêtes ratent le top-10 quelles que soient les pondérations. | (1) Avant tout grid-search, faire tourner un ablation test : si désactiver un paramètre ne change rien, il est mort → supprimer le code, pas l'inclure dans la grille. (2) Quand la pondération sature, le gain suivant ne vient **pas** du tuning mais du reranking (cross-encoder) ou de l'enrichissement texte (re-OCR des scans). (3) Runner grid : toujours passer par un endpoint batch (une seule récupération Qdrant + fusions locales) sinon 41 × 145 × 4s = 6.6h au lieu de 15 min. |
| 2026-04-19 | A/B baseline rerank-OFF : 37/145 requêtes renvoient `/search` 500 sans traceback dans uvicorn. Cause racine = Colab `/encode` origine HTTP 502 Bad Gateway (tunnel CF vivant, auth OK, process FastAPI mort). Impossible à voir dans les logs Mac car `server/main.py:217` ne log que `str(exc)` dans la réponse JSON, pas le traceback. | (1) **Health-check obligatoire avant A/B 145 queries** : `curl https://encode.jinkohub.digital/healthz` avec CF Access headers ET `User-Agent: DocFinder/1.0` (UA par défaut urllib = bot rule 1010 sur CF). Si 502/5xx → stop, restart Colab, don't run. (2) **Log traceback côté serveur** : dans `server/main.py` `except Exception as exc:`, faire `logger.exception(...)` avant le `return JSONResponse(...)` — sinon debug à l'aveugle en prod. (3) Distinguer CF 502 (origine morte) vs 1010 (UA bot) vs 530 (tunnel down) avant de blâmer le code applicatif. |
| 2026-04-19 | 2ᵉ tentative baseline juste après restart Colab : health-check 200 OK à t=0, lancement 145 queries, le backend meurt après ~20-30 queries → 121/145 en 500 (RemoteEncoderError HTTP 530 puis 502). Un health-check ponctuel ne prouve pas la stabilité sous charge. | Avant un run A/B long (145 × ~3s), faire un **burst test** : 20 requêtes `/search` consécutives en < 30s. Si tout passe → lancer le run complet. Sinon, le backend n'absorbe pas la charge (GPU OOM, runtime idle kill, ou tunnel CF throttle) → investiguer côté notebook avant de brûler des tokens. |
| 2026-04-19 | Baseline rerank-OFF clean acquise (MRR@10 = 0.7179). Test `curl -X POST https://encode.jinkohub.digital/rerank` → HTTP 404. Le reranker BGE-reranker-v2-m3 n'était pas exposé sur le tunnel Colab. Lancer le variant rerank-ON dans cet état donne une comparaison non significative : `server/search.py::rerank_results()` a un `except Exception` qui renvoie l'ordre fusion inchangé → ΔMRR ≈ 0, mais le CSV semble valide. | Avant un run A/B rerank, vérifier explicitement **l'endpoint `/rerank`** (pas seulement `/healthz` ni `/encode`) : `curl -X POST -H "X-Auth-Token: $TOKEN" ... /rerank -d '{"query":"test","passages":["a","b"]}'` → doit retourner 200 avec des scores. Le reranker est un service séparé de l'encoder. Corollaire : quand un fallback silencieux existe, ne jamais l'interpréter comme un résultat — toujours tester l'endpoint live avant le run. |
| 2026-04-19 | Premier wrapper `QwenEmbedderWrapper.encode()` passait `max_length=512` en kwarg à `SentenceTransformer.encode()` → `TypeError: encode() got an unexpected keyword argument 'max_length'`. FastAPI convertit en HTTP 500 "Internal Server Error" nu, pas de traceback côté probe curl. Erreur visible uniquement dans le stderr uvicorn Colab. | `SentenceTransformer.encode()` n'accepte **pas** `max_length` comme kwarg — piloter la troncature via l'attribut `m.max_seq_length = max_length` avant l'appel (applicable à tout le batch). Règle générale : après écriture d'un nouveau wrapper encoder, ne pas se fier à l'import/instanciation réussi — lancer un probe end-to-end (`curl /encode_qwen` avec 1 texte) immédiatement et vérifier que la réponse contient `dense` non-vide avant d'assumer que le service est prêt. |
| 2026-04-19 | Plusieurs heures perdues à debug "uvicorn meurt juste après /health 200" — spawné via nohup, disown, screen, Python `os.setsid()`, Bash `run_in_background` : chaque fois le process mourait quelques secondes après startup. Cause racine = plugin MCP `context-mode@context-mode` installé globalement : il intercepte les `curl/wget` des tool Bash de Claude Code, injecte un message "blocked — use ctx_execute" et kill les subprocess longs. Invisible dans stdout/stderr, seul indice = un message "context-mode: curl/wget blocked" qui apparaît à la place du résultat réel. Aussi responsable du persisted-output préambule 2KB au début de chaque gros tool result. | (1) Si un subprocess long meurt mystérieusement après un probe `/health`, suspecter un **plugin MCP** actif avant n'importe quoi d'autre (SIGHUP, OOM, macOS reaper). Lister les plugins activés dans `~/.claude/settings.json > enabledPlugins`. (2) Désactiver `context-mode@context-mode` pour DocFinder (projet FastAPI local sans besoin de fetch externe). (3) `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50` force un compactage à 50% du contexte — passer à **92** (standard). Avec 50, compactage toutes les 3 min sur tâche chargée. |
| 2026-04-19 | `"context-mode@context-mode": false` dans `~/.claude/settings.json` **ne tue pas** les 2 node processes `start.mjs` du plugin en cours d'exécution. Ils continuent à intercepter les subprocess locaux ; seul un restart complet de Claude Code les reap. Dans cette session : après `false`, `pgrep -f "context-mode/start.mjs"` retournait toujours 2 PIDs (79739, 74515), uvicorn continuait de mourir. Il a fallu `kill` manuel des deux PIDs pour que les probes `curl` locaux cessent d'être interceptés. | Après avoir désactivé un plugin dans `enabledPlugins`, **toujours** vérifier `pgrep -lf "<plugin>/start.mjs"` (ou équivalent) et kill manuel si présent. Alternative : restart complet de Claude Code. Ne pas faire confiance à `"false"` seul pour un plugin déjà lancé. |
| 2026-04-19 | Mesure A/B Plan 1 (rerank cross-encoder BGE-reranker-v2-m3 sur top-10) : **ΔMRR = −0.0996** (off 0.7179 → on 0.6182), 37 losses vs 17 wins, 91 inchangées. Hypothèse : `rerank_results()` **remplace** l'ordre fusion par l'ordre reranker → perd tout le signal filename/title boost amont. BGE-reranker-v2-m3 entraîné sur paires courtes, nos chunks sont longs (2000 chars). Réflexe attendu "reranker multilingue puissant ≥ fusion RRF bien tunée" = **faux** sur ce corpus. | (1) Ne jamais activer un cross-encoder reranker sans A/B quantifiée (145 queries min avec ground-truth). (2) Si on veut retenter : fusion pondérée `α·fusion_score + (1−α)·rerank_score` plutôt que remplacement ; grid α ∈ {0.3, 0.5, 0.7, 0.9}. (3) Considérer un reranker entraîné sur long-context (ex: bge-reranker-v2-minicpm-layerwise) ou un rerank top-N plus grand (top-30) pour laisser le modèle trier plus de candidats. (4) Tant que non re-tuné, garder `RERANK_ENABLED=false` par défaut — code et flag restent en place, neutres pour l'utilisateur. |
| 2026-04-19 | `SearchEngine.__init__` appelait `Embedder.get_instance()` de manière eager → chargement de `multilingual-e5-large` (~1 GB RAM) même quand `COLAB_ENCODE_URL` est set et que tous les encodings passent en remote. Sur Intel Mac 16 GB saturé, ce chargement met le système en pression mémoire (pas nécessairement fatal — hypothèse jetsam initialement invoquée à tort, cf. L#30 ci-dessous). Même si la cause racine du crash était ailleurs, l'eager load reste du gaspillage pur quand on est stateless. | `SearchEngine` charge maintenant l'embedder local **uniquement si `COLAB_ENCODE_URL` n'est PAS défini** (stateless Mac complet). L'accès se fait via une @property lazy → fallback transparent pour les cas de test unitaire ou dev sans Colab. Règle générale : tout modèle ≥ 100 MB doit être lazy-chargé, pas constructeur. |
| 2026-04-19 | Colab `colab_helpers_cell.py` : quand le Mac uvicorn tombe (502 via CF tunnel, ou connection refused pendant restart LaunchAgent), le thread pipeline mourait sur une exception unique et il fallait **rerun cell 6 à la main**. Problème quand l'utilisateur est absent : pipeline figé jusqu'au retour. | Wrapper `_run_pipeline_threaded()` dans une boucle try/except avec **backoff exponentiel** (15s → 30s → … → 300s max) : sur toute exception du pipeline, log et retry. Sort proprement sur `KeyboardInterrupt` (bouton Stop cellule). Le notebook est maintenant self-healing pour toute coupure Mac transitoire. |
| 2026-04-19 | **Vrai killer du uvicorn :8000** identifié après heures de chasse : deux instances de `docfinder/start.sh` lancées le vendredi matin par l'utilisateur, **jamais arrêtées**, tournaient en boucle `while true` avec `lsof -ti tcp:8000 \| xargs kill -9` toutes les 30s (fonction watchdog du script). À chaque kill, le LaunchAgent respawnait uvicorn en 10s → cycle 34s parfaitement régulier observé dans `launchctl print`. Identifié via `launchd` unified log `sent by kill[<PID>]` + `pgrep -lf start.sh` qui a révélé les 2 fantômes PID 87848/87760 (ELAPSED 3 jours). Initialement blâmé à tort : context-mode MCP, RAM pressure, jetsam. | (1) Toujours démarrer par `launchctl print gui/$UID/<service>` quand un LaunchAgent se fait tuer en boucle — la ligne `last terminating signal = Killed: 9` + `sent by kill[<PID>]` dans `log show --predicate "eventMessage CONTAINS '<service>'"` **donne directement le PID du killer**. (2) `start.sh` doit avoir un trap EXIT ou un `.pid` file pour éviter les instances dupliquées qui tournent pendant des jours. (3) **Ne jamais blâmer la RAM / jetsam sans preuve du log** (jetsam loggue son activité explicitement avec "memorystatus_kill"). (4) Après un `ps aux` suspect, toujours cross-référencer avec le timestamp du crash — ici les start.sh avaient 3+ jours d'uptime, évident si on avait regardé plus tôt. |
| 2026-04-19 | 13 des 22 requêtes ratées de la baseline A/B (MRR 0.7179) ciblent des **scans PDF** (attestation-ameli, prévoyance axa, URSSAF, fiches paie, télétravail, résiliation parking, questionnaire lycée, etc.). Le seuil d'OCR de `colab/extractor.py` était à `<10 chars/page` : une page scan avec un simple en-tête imprimé "Page 1 / Mairie de Paris" (~25 chars) passait à travers sans OCR, perdant tout le contenu utile. Pas visible dans les logs car l'OCR ne se déclenchait juste pas. | (1) Seuil page OCR remonté à **100 chars** (`PAGE_OCR_MIN_CHARS`) : couvre les en-têtes typiques de scans tout en évitant les faux positifs sur les vraies pages quasi-vides. (2) Ajout d'un **filet de sécurité doc-level** (`DOC_OCR_FALLBACK_CHARS = 300`) : si après la 1re passe le texte total du PDF reste famélique, OCR forcé sur toutes les pages restantes. (3) Règle générale : un seuil d'OCR ne doit jamais se fier uniquement à une métrique page-level ; les scans multi-page ont souvent des en-têtes imprimés + contenu en image. Toujours prévoir un filet doc-level. |
